# Rastera Engine — Python dependencies
# Pin major versions; allow patch updates in production

# Web framework
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# Settings & validation
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# Database — async SQLAlchemy + PostGIS
sqlalchemy[asyncio]>=2.0.28,<3.0.0
geoalchemy2>=0.14.7,<1.0.0
psycopg[binary]>=3.1.18,<4.0.0      # psycopg3 — async-native, no greenlet needed

# HTTP client (geocoding, external APIs)
httpx>=0.27.0,<1.0.0

# OpenAI
openai>=1.30.0,<2.0.0

# PDF generation
weasyprint>=62.3,<63.0
jinja2>=3.1.4,<4.0.0

# Geospatial
shapely>=2.0.4,<3.0.0
pyproj>=3.6.0,<4.0.0

# Rate limiting
slowapi>=0.1.9,<1.0.0
limits>=3.7.0,<4.0.0

# Background scheduler (worker container)
apscheduler>=3.10.4,<4.0.0

# Env
python-dotenv>=1.0.1,<2.0.0
